import React from "react";
import { Button, Form, Input, Modal, Select } from "antd";

const ProductForm = (props) => {
    return (
        <Button primary style={{float: 'right'}}>New Product +</Button>
    )
}

export default ProductForm;